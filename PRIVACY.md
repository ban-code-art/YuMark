# YuMark 隐私政策

最后更新：2026-09-06（v0.11）

YuMark 是一款**本地优先**的 Android Markdown 笔记应用。本政策说明你的数据存放在哪里、
什么情况下会离开你的设备。一句话概括：**本应用不收集、不上传、不出售任何用户数据。**

## 1. 我们收集什么？

什么都不收集。应用本体：

- **没有**任何统计/分析 SDK（无友盟、Firebase Analytics 等）；
- **没有**任何崩溃上报 SDK（无 Crashlytics、Sentry 等），见
  `core/crash/CrashReporter` 的设计声明；
- **没有**广告、追踪器或设备指纹；
- 不请求通讯录、位置、麦克风、相机等任何敏感权限。

## 2. 数据存在哪里？

| 数据 | 位置 |
| --- | --- |
| 笔记正文与图片 | 应用私有目录（`filesDir`） |
| 文档数据库（含历史版本、全文索引） | 应用私有 Room 数据库 |
| AI / WebDAV 密钥与密码 | Android Keystore 主密钥保护的加密存储（`EncryptedSharedPreferences`，AES256-GCM） |
| 崩溃日志 | 应用私有目录 `crash/`，最多 20 条 / 512KB，滚动覆盖 |

系统云备份（Android Auto Backup）中，**加密密钥文件被刻意排除**（主密钥绑定设备，
备份它们只会带去无法解密的密文）；其余数据的备份遵循系统设置，可随时在系统设置中关闭。

## 3. 什么情况下数据会离开你的设备？

只有两种，且都由你主动发起或明确授权：

1. **WebDAV 同步**（你配置服务器地址与凭据后）：笔记正文发送到**你自己指定的**
   WebDAV 服务器。应用不内置任何中转服务器。
2. **AI 功能**（你配置 API 端点后）：相关笔记内容（整篇文档或相关片段）随你的请求
   发送到**你配置的**第三方 AI 服务端点，用于生成回复或建立本地知识索引。首次启用时
   应用会单独弹出知情确认。数据是否被该服务存储或用于训练，取决于其服务条款。

## 4. 唯一的自动网络请求

应用启动时可向 `api.github.com` 查询是否有新版本。该请求**不含任何个人数据、
设备标识或笔记内容**（User-Agent 仅标识应用名）。你可以在「设置 → 启动时自动
检查更新」中随时关闭；关闭后手动检查仍可用。

## 5. 崩溃日志

崩溃日志只保存在本机，供你排查问题使用。你可以在「设置 → 崩溃日志」中查看、
导出（SAF 导出到你选定的位置）或清空。**导出前会自动抹除**疑似 API Key、
Bearer Token、URL 内嵌凭据等内容。

## 6. 第三方组件

应用使用的第三方库（Compose、Room、Ktor、Coil、commonmark 等）均为本地运行的开源
组件，不向其作者传输你的数据。Markdown 渲染脚本（marked、KaTeX、Prism、Mermaid、
DOMPurify）随应用打包在本地，渲染在设备内完成，加载远程图片除外（由笔记内容中的
图片链接决定，遵循各图床自身政策；导出 HTML 默认带 `no-referrer`）。

## 7. 儿童隐私

本应用不面向 14 岁以下儿童，也不有意收集任何用户的个人信息（包括儿童）。

## 8. 政策变更

实质性变更会随版本发布说明（GitHub Releases）公布。继续使用即视为接受更新后的政策。

## 9. 联系方式

通过项目仓库的 Issue 联系：<https://github.com/ban-code-art/YuMark>
