# Changelog

YuMark 的版本变更记录。本文件自 v0.11 起恢复维护：此前的变更摘要见
[GitHub Releases](https://github.com/ban-code-art/YuMark/releases) 与 git 提交历史。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；
版本号与 `app/build.gradle.kts` 的 `versionName` 对应。

## [0.11] — 2026-09-06

企业级上线补齐：回收站 / 图片同步 / 后台同步 / 隐私合规 / 体验与一致性修复

### 新增：回收站（删除兜底）
- 「删除文档」改为**移入回收站**（软删除）：库行、正文、图片、历史版本全部保留，可随时恢复；这是此前唯一「一次操作即永久丢失」的路径（无回收站、版本史随删除级联、远端文件被同步删除）。
- 新增回收站页（文件列表顶栏入口）：恢复、彻底删除（二次确认）、清空回收站；移入 30 天的文档自动彻底清理。
- 恢复时原名被占用会自动改名落座（`笔记 (2)`）；原文件夹已删除时落在根目录。
- 数据库迁移 v13 → v14（`documents` 新增 `deleted_at` / `original_name` 两列，ADD COLUMN 全程零数据变动）。
- 文件夹删除同样改为「子树进回收站」；彻底删除发生在回收站的显式操作或到期清理时，那时才立同步墓碑、删远端文件。
- 同步的「远端删除传播到本机」也从硬删除改为移入回收站，本机多出一条应用内恢复路径。

### 新增：WebDAV 后台自动同步
- WorkManager 周期任务：每 6 小时联网条件下自动同步一次；「同步设置」里保存配置即时生效（开 = 注册，关 = 注销）。
- 同步未启用/配置不完整时后台任务直接空转返回，不消耗重试预算；瞬时失败指数重试（≤3 次）。

### 新增：隐私合规
- 新增隐私政策：仓库根目录 [PRIVACY.md](PRIVACY.md) + 设置页「隐私政策」入口（应用内摘要）。
- AI 知情确认：首次启用 AI 时弹窗说明「笔记内容将发送到你配置的第三方端点」，同意后置位并随配置备份走；已启用但未确认的老用户进 AI 配置页时补弹。
- 更新检查开关：设置页新增「启动时自动检查更新」（默认开）——这是应用唯一一处非用户主动发起的网络接触，现在可以一键关闭；关闭后手动检查不受影响。

### Agent 能力智能化
- **read_document 支持大纲模式与字符分页**：`mode=outline` 快速了解大文档结构，`offset/length` 分页读取目标段落——此前大文档只有前 6000 字符对模型可见，尾部内容永远无法触达
- **list_documents 输出文件夹结构与名称路径**：文档的「所在文件夹」从 UUID 变为 `工作/项目A` 式名称链，模型第一次能真正感知库的目录组织
- 工具 schema 同步更新，Agent 系统提示的大文档导航指引随之生效
- **任务计划状态注入系统提示**：每轮请求重建系统提示并注入当前计划的实时状态（目标 + 各步骤勾选态）——上下文裁剪可能丢掉早期的 update_plan 调用，注入让模型在长任务后半程不"失忆"、不偏离目标
- 工具执行器改为直接使用仓储携带的正文（少一跳文件 IO），顺带消除一处 domain→data 分层违规

### 性能与内存
- **Agent 上下文窗口管理**：长会话/长任务运行不再撞爆模型上下文——每轮请求前按 token 预算裁剪历史（CJK 感知估算、按回合组整体丢弃、工具调用配对永不拆散、最近工具链受保护区；单条超长正文不静默截断）
- **emoji 渲染修复**：Markdown 预览、AI 消息气泡、设置更新日志的字体栈补齐 emoji 回退（Noto Color Emoji 等），修复部分设备上 emoji 显示为方框的问题
- **历史版本流按需订阅**：仅在历史面板打开时才装载版本（1MB 文档一进编辑器即常驻 ~50MB 全文快照的问题消除）。
- **图片解码绝对分辨率护栏**：压缩关闭时 20MP+ 图片不再全尺寸解码（~80MB 位图 OOM 风险），按 4096 最长边下采样。
- **孤儿图片清理**：启动期后台自动清理「库行已删、文件残留」的孤儿图片（原为死代码，激活）。
- 外部文档保存：写盘前检测 lastModified 基线，外部应用在打开期间改过文件时先把外部现值备份为冲突副本再写入（两个版本都不丢）；文件读写仍为非原子（SAF 限制）。

### 修复
- **同步与编辑器的盲写互覆盖（高危）**：编辑器此前只一次性加载文档、从不观察仓库——同步把远端内容写进数据库后，下一次自动保存会用内存旧内容把它顶掉；而 DownloadOverwrite 已刷新同步基线，这个覆盖会被当作「干净的本地修改」直接上传，远端版本静默丢失。现在编辑器订阅文档流：无未保存修改时自动采纳外部版本（热替换刷新界面）；有未保存修改时远端版本自动存为冲突副本「(冲突 …)」并提示，两侧内容都不丢。
- 删除文档从不清理 RAG 知识索引的旧缺口：移入回收站/彻底删除都会把分块、向量与内存检索表一并摘掉，恢复后自动重建索引。
- 双窗格下文档从列表侧移入回收站后，右侧编辑器的自动保存会把该文档反复写回全文索引与 RAG 索引（烧 embedding 调用且知识检索可命中已删文档）——保存链路现在按回收站标记跳过索引写入。
- **自动保存的脏标记竞态（静默丢字）**：写盘挂起期间新敲的字被无条件清脏吞掉，此后防抖/轮询/退出保存全部跳过，直到再次输入才可能落盘——清脏改为内容比对式，且脏标记加 @Volatile 保证跨线程可见。
- **退出时保存失败仍无条件离开编辑器**：SAF 外部文档写失败（网盘断开/授权回收）时脏内容静默蒸发——返回键与侧栏切换现在按保存结果门控，失败留在编辑器并展示原因。
- **连续删除时撤销提示丢失**：前一条 Snackbar 因新提示顶上来被取消，其清理回调把刚挂上的新撤销项一并吞掉——撤销状态改为按 id 对账的超时自清（11s，与 Snackbar Long 档对齐），免疫该竞态。
- **AI 划词替换 / 版本恢复不再清空撤销历史**：整篇替换改用强制边界记录（替换前的状态进撤销栈），AI 改错一个词现在可以直接 Ctrl+Z 撤回，不必翻版本历史。

### 新增：图片/附件同步（`_media/` 通道）
- 独立于文档同步的媒体通道：本地图片批量推送到远端 `_media/` 子目录（单轮上限 50 张，余量下轮续推）；文档从远端落地（新建/覆盖/冲突副本）时按正文 `images/<uuid>.<ext>` 引用逐张精确拉取并登记。
- 解决「换设备后图片全部裂图」的问题。远端孤儿图片暂不清理（多设备安全取舍，见 `MediaSync` 类注释）。

### 体验优化
- **删除可撤销**：移入回收站后弹出 Snackbar，10 秒内点「撤销」原样拿回——文件列表与编辑器两个删除入口都支持。
- **编辑器内删除当前文档时立即退出**：不再让用户停在一篇已进回收站的文档里继续输入。
- **回收站入口角标**：文件列表顶栏的回收站图标显示未清空条数，删过的内容一眼可见。
- **启动即同步**：开启 WebDAV 的用户打开应用即静默增量同步（哈希比对，未变化零传输），不必等周期任务或手动触发。
- 同步全程互斥锁：手动/周期/启动三个触发点并发时排队执行，消除基线乱序回写的窗口。

### UI 一致性
- 回收站页与文档库视觉语言对齐：卡片行（标题 + 「N 字 • 移入于 N 天前」元信息）、列表动效 `animateItem`、降饱和图标的空态锚点。
- 相对时间抽为公共组件：文档卡片与回收站同一套口径（语区/复数/边界不再可能漂移），并补上「8 周以上退绝对日期」分支。
- 文件列表与搜索的空态加降饱和大图标锚点，与回收站空态同一手法。
- 通过项目自带的 WCAG 对比度审计（120 对 0 不达标）与多语言字符串一致性检查（FATAL/WARN 均 0）。

### 工程化
- 新增签名发布流水线 `.github/workflows/release.yml`：打 `v*` 标签自动构建签名 release APK、计算 SHA-256 并发布 GitHub Release（签名材料经仓库 Secrets 注入，不入库）。
- 接入 detekt 静态分析（默认规则集 + 存量基线 `config/detekt-baseline.xml`），CI 只拦新增违规。
- `CHANGELOG.md` 恢复维护（本文件）。

## [0.10] — 2026-09-05

release: v0.10 — 企业级收口 / 同步删除救援 / 导出管线图片补完 / RAG 自定义端点
- WebDAV 删除传播前落 `sync_trash` 救援副本（有界 100 份 / 64MiB，设置页可查看导出）。
- 导出管线补完：HTML 导出内联本地图片（目录白名单 + 6MB 上限 + 路径消毒）、长图 16000px 硬上限、PDF 逐页绘制。
- RAG 支持独立 embedding 端点（不复用 chat 地址时不再静默回落）。
- 单元测试 1238 个；CI 增加 Room schema 提交一致性守卫。

## [0.9.1] — 2026-09

release: v0.9.1 — 划词 YY 触发 / 删除匹配增强 / 创建文档内容丢失修复

## [0.9] — 2026-09

release: v0.9 — Agent 能力扩展 / 对话滚动修复 / 手动停止

## [0.8] — 2026-09

release: v0.8 — 导出补完 / 历史版本 / 平板折叠屏 / WebDAV 同步 / Agent 重写

## [0.7] — 2026-09

release: publish v0.7 — Agent 稳定性 / AI 多模态 / 原子写入 / 签名配置本地化
（详见仓库内 v0.7 Release Notes）

## [1.0.0] — 2024

首次发布：Markdown 所见即所得编辑、文件管理、全文搜索、导出、主题。
